When creating screen shots for classes in the screenshot-maven-plugin-api it self you
must use a launch config that specifies the goal without using a goal prefix. Instead
you must use the form: groupId:artifactId:goal